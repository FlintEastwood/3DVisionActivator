
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include "system/nvidiashutterglasses.h"
#include "system/app.h"

#include "sharedShutterState.h"

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class MainApp : public App
{
public:
    MainApp(const sf::VideoMode& videoMode, const std::string& title);

private:
    void processEvent(sf::Event event);
    void update(float dt, const sf::Keyboard& input);
    void render();

	SharedShutterState sharedShutterState;
	NvidiaShutterGlasses shutterGlasses;
	sf::VideoMode videoMode;
	sf::Thread subThread;
	bool flipEyes;

	friend void createOffscreenRenderAppThread(void* data);
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
