#pragma once

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include <SFML/Window.hpp>

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class App
{
public:
    App(const sf::VideoMode& videoMode, const std::string& title);
    void run();

protected:
    sf::Window window;
    sf::Clock clock;
    sf::Keyboard sfKeyboard;

	float fov;
    float nearZ;
    float farZ;

    void resize(int width, int height);

private:
	virtual void processEvent(sf::Event event) = 0;
	virtual void update(float dt, const sf::Keyboard& input) = 0;
    virtual void render() = 0;
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
