#pragma once

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#include "system/shutterglasses.h"
#include "system/app.h"

#include "sharedShutterState.h"

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

class OffscreenRenderApp : public App
{
public:
    OffscreenRenderApp(const sf::VideoMode& videoMode, SharedShutterState& sharedShutterState);

private:
	void processEvent(sf::Event event);
	void update(float dt, const sf::Keyboard& input);
    void render();
	void resize(int width, int height);

	SharedShutterState& sharedShutterState;
	ShutterGlasses shutterGlasses;

	float yRotation;
};

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
